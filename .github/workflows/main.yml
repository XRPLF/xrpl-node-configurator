name: Sync Fork

on:
  schedule:
    - cron: '0 1 * * *' # every 30 minutes
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: XRPLF/xrpl-node-configurator@latest
        with:
          owner: llvm
          base: master
          head: master
